#include "main.h"

/**
 * add - Function that adds two integers and returns the result
 * @x: int type number
 * @y: int type number
 * Return: result of addition
 */
int add(int a, int b)
{
	return (a + b);
}
